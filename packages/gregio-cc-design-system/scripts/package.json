{
  "name": "gregio-cc-design-system-scripts",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Node helpers for the design-system pipeline skills. Dependencies are NOT installed here — they are resolved on-demand via `npx --yes --package=...`.",
  "engines": {
    "node": ">=20"
  },
  "x-runtime-deps": {
    "note": "Not real dependencies — documented here so the user/agent knows which packages to pass to `npx --package=...`.",
    "always": [
      "playwright@1.58.2"
    ]
  }
}
