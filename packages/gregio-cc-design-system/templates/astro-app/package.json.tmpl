{
  "name": "{{VALUE:app-name}}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port {{VALUE:port}}",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port {{VALUE:port}}",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.8"
  }
}
