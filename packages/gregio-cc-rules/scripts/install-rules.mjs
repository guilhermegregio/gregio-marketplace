#!/usr/bin/env node
// Deprecado: o instalador virou subcomando do engine kb (cli/kb). As rules seguem aqui.
console.error('install-rules.mjs foi removido — use: kb rules <repo> [--stack a,b] [--dry-run] [--prune] [--list]');
process.exit(1);
